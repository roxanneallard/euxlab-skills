# In progress

Beta. Skills here are public on purpose: try them and report what breaks. They stay out of the plugin and the top-level README until they move to a promoted bucket. They get no docs page, and they can change or disappear without warning.

The plugin won't give you these. Install one directly:

```bash
npx skills@latest add roxanneallard/euxlab-skills --skill=<name>
```

This bucket is empty. The next skills on the roadmap land here first.
